// Follow up to question 2, what if the given array S is already sorted

#include "../../bits/stdc++.h"
using namespace std;

int main(void)
{

    // Same solution as Q2, just remove the mergeSort, merge functions, and don't sort the array

    return 0;
}