// Determine the length of the longest increasing contiguous sub-array in S.

#include "../../bits/stdc++.h"
using namespace std;

int main(void)
{

    // Pretty easy, but too lazy to do it.
    // Simply run a O(n) loop to check if arr is already sorted, if it is, print it all out
    // if not, then run a loop that is slightly O(n^2) that checks for bigger contiguous
    // sub-arrays, and stores them in a vector, then, in the end, finally print the vector out

    return 0;
}