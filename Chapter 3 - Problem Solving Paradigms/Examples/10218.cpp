#include "stdc++.h"
using namespace std;

int main(void)
{
    int t ;
    cin >> t;

    while ( t-- )
    {
        int a, b, c;
        cin >> a >> b >> c;

        if ( a + b < c )
            continue;
        else
        {
            cout <<
        }
    }

    return 0;
}